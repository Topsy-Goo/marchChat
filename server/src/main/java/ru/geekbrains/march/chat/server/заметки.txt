
Материалы двух лекций (6 и 7) находятся в файлах ServerApp, Server, ClientHandler, window, Main, Controller.

ДЗ-6 находится в файлах ,названия которых начинаются с hw

ДЗ-7 находится в файлах ,названия которых начинаются с hw7

После сдачи ДЗ-x файлы ДЗ-x почти не редактировались, вся дополнтельная правка делалась в
 файлах лекций. Это справедливо по крайней мере для ДЗ-7.

Кроме того ДЗ-7 сперва было сделано на базе файлов лекций, а потом скопировано с минимальными
 изменениями в отдельные файлы (начинающиеся на hw7) для выгрузки в репо.


----

файлы с 8-кой в конце имени содержат ДЗ-8, в котором реализован вход юзера в чат по логину и паролю.
 файлы без 8-ки содержат ДЗ-8, в котором юзер входт в чат по вводу любого уникального и непустого имени.

оба варианта нужно оставить, т.к. не известно, кторый из них пригодится. Вход по логину и паролю
 выглядит преждевременным для такой примитивной реализации чата, но он был в условии ДЗ, поэтому
 его пришлось оставить.

Остальные варианты удалены за ненадобностью.

-----
Файлы без 8-ки изменены:
 - Добавлена авторизация при пом. аутентификатора;
 - Закомментированы проверки соединения (теперь потоки закрываются по исключению при закрытии соответств. сокета);
 .
----------
Подписываем проект server на совместимость с JDBC.
 Переписали, сделали копии файлов для ДЗ (они с 9-кой на конце; все кроме Authentificator и
 JdbcAuthentificationPrivider'а) и отправили в Гит. Думаю, теперь можно «9-ки» удалять.
 Удалили «8-ки».
--------------
Устранили ошибки перезда на JDBC.
 Добавили возможность сохранения истории чата в файл.
 Сейчас годятся для сдачи в составе ДЗ-3 файлы:
 - Server9
 - ServerApp9
 - ClientHandler9
 - Authentificator
 - JdbcAuthentificationProvider
 - Main9
 - window9.fxml
 - Stenographer
 - MessageStenographer
 - Controller9
 Привели основные файлы и 9-ки к общему виду, наделали из 9-ток 10-ки и удалили 9-ки.
--------------------
Максимально изолировали JavaFX от основного кода чата: поток чтения сетевого канала слушает канал и складывает
 услышанное в очередь и никак не пересекается с интерфейсом. Поток чтения очереди забирает сообщения из очереди
 и обрабатывает их по одному в потоке JavaFX (сам тоже кроме очереди ничего не трогает). Т.о. вся работа
 остальная работа приложения выполняется в потоке JavaFX, т.е. в работу потока JavaFX другие потоки не
 вмешиваются.
 + дороботали интерфейс -- добавили кнопку Выход, а то Jfx, похоже, плохо ориентировалась, в каком контексте
  её нажимают, т.к. запросы на обработку приходили немного несвоевременно, вразнобой.
--------------------
